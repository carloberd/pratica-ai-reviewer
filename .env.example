# Credenziali OAuth 2.0 (tipo "Desktop app" oppure "Web application" con redirect su 127.0.0.1)
# creati nella Google Cloud Console del progetto usato dal collega.
# Questo file NON deve mai finire su git: copiarlo in `.env`.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
